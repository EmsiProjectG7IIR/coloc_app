import 'package:flutter/material.dart';

class DemandPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('No demands currently.'),
      ),
    );
  }
}

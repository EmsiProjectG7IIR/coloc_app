server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/springBootColoc
    username: postgres
    password: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        ddl-auto: update